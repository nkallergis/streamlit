import streamlit as st
from helper import utils

utils.load_sidebar()

"""
### Git: Init a repo

```
1. Create the repository in GitHub and copy its URL
2. On bash do:
  git init
  git add .
  git commit -m 'Initial commit'
  git remote add origin <REPO_URL>
  git push -u origin master
```
"""
